# NexaSphere Mobile (Expo - React Native)

## Run

- Install deps from repo root:
  - `npm install`
- Start the app:
  - `cd mobile && npx expo start`

## Test

- `cd mobile && npm test`

> Note: This is the initial scaffold to satisfy #1806. Further feature parity with the web app should be implemented incrementally.
